
set(PACKAGE_VERSION "@MINVR_VERSION_FULL@")

if ("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@MINVR_VERSION_MAJOR@")
	set(PACKAGE_VERSION_COMPATIBLE TRUE)
	if ("${PACKAGE_FIND_VERSION_MINOR}" EQUAL @MINVR_VERSION_MINOR@)
		set(PACKAGE_VERSION_EXACT TRUE)
	endif()
else()
	set(PACKAGE_VERSION_COMPATIBLE FALSE)
endif()

